# Agent Guidelines

- Use the `codex-max` model for reasoning and code generation when applicable.
- Keep the project configured for Java 21 with Gradle (Groovy DSL).
- Favor clear, readable, and compact implementations that follow clean code principles.
